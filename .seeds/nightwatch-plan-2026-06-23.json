{
  "template": "refactor",
  "name": "Nightwatch patrol 2026-06-23: duplication + logic bug fixes",
  "sections": {
    "context": "Automated nightwatch patrol on 2026-06-23 identified: (a) one logic bug in auto-plan-run dispatch that could silently skip a newly-created plan when an agent replaces an old plan in the same run (workspacePlanIds.size <= baselinePlanIds.size early-exit is wrong), and (b) widespread code duplication across the server and CLI — formatError defined 22 times as private functions, GitHub REST helpers (buildHeaders/readText/readJson) duplicated between pr-annotate.ts and pr-checks.ts, env-parsing utilities (parseEnvDuration/parseEnvPositiveInt/isTruthy) duplicated between preview/eviction/config.ts and runs/reap/gc.ts, identical defaultGitCredentialsRun/defaultGitIdentityRun implementations in two supervisor files, repeated read-yaml-validate pattern inside warren-config/load.ts, resolveTargetDir duplicated between two CLI commands, and test harness infrastructure duplicated between projects.test-helpers.ts and runs.test-helpers.ts.",
    "behavior_invariant": "All existing tests must continue to pass without modification. The auto-plan-run fix must not change behavior when baseline and workspace differ only in size (new-plan-added or no-change scenarios). Refactored utility references must resolve to the same logic they replaced — no behavior changes, only consolidation of call sites.",
    "approach": "Fix the auto-plan-run bug first (logic correctness, standalone, minimal risk). Then consolidate formatError into a shared utility. Then unify the duplicated GitHub REST helpers by importing from pr-checks.ts. Then consolidate env-parsing utilities (parseEnvDuration / parseEnvPositiveInt / isTruthy) shared between preview-eviction and gc. Then deduplicate supervisor git-runner utility. Then extract the internal read-yaml-validate helper in warren-config/load.ts. Then extract resolveTargetDir CLI utility. Finally consolidate the test harness boilerplate. Each step is a single coherent PR.",
    "steps": [
      {
        "title": "Fix auto-plan-run early-exit: replace size comparison with new-plan detection",
        "description": "File: src/runs/reap/auto-plan-run.ts, line 212.\n\nThe early-exit guard `workspacePlanIds.size <= baselinePlanIds.size` incorrectly skips dispatch when an agent deleted an old plan and created a new one in the same run (net-zero size change). The only sound check is whether workspace contains any plan ID that baseline does not.\n\nFix: remove the size check from the early-exit, and instead compute the set difference inside the loop or at the top of the function:\n\n```ts\n// before\nif (\n  workspacePlanIds === null ||\n  baselinePlanIds === null ||\n  workspacePlansBody === null ||\n  workspacePlanIds.size <= baselinePlanIds.size\n) {\n  return { created: false, id: null, planId: null };\n}\n```\n\n```ts\n// after\nif (\n  workspacePlanIds === null ||\n  baselinePlanIds === null ||\n  workspacePlansBody === null\n) {\n  return { created: false, id: null, planId: null };\n}\n// The loop already skips baseline plans via `if (baselinePlanIds.has(planId)) continue;`\n// so no additional early-exit is needed — let the loop do the work.\n```\n\nAdd a unit test case: baseline=[pl-old], workspace=[pl-new] (both size 1), expects autoPlanRunCreated=true.",
        "type": "bug",
        "priority": 2,
        "blocks": [],
        "labels": ["nightwatch"]
      },
      {
        "title": "Consolidate formatError into a shared core utility",
        "description": "The function `function formatError(err: unknown): string` is defined identically (or near-identically) as a private helper in at least 22 source files across src/seeds-cli/, src/server-config/, src/cli/, src/warren-config/, src/registry/, src/plan-runs/, src/triggers/, src/projects/, src/plot-plan-runs/, src/runs/, src/plots/.\n\nFix:\n1. Add `export function formatError(err: unknown): string` to `src/core/errors.ts` (already the error hierarchy home).\n2. Delete all 22 private `formatError` definitions.\n3. Replace each call site with an import from `../../core/errors.ts` (path varies by depth).\n4. Verify `bun run lint && bun run typecheck && bun test` pass.\n\nNote: `src/cli/output.ts` already exports one. That export should be replaced by a re-export from core so external callers don't shift.\n\nThe UI's `src/ui/src/lib/format-error.ts` is separate (different package, different context) — leave it untouched.",
        "type": "task",
        "priority": 3,
        "blocks": [],
        "labels": ["nightwatch"]
      },
      {
        "title": "Remove duplicated GitHub REST helpers from pr-annotate.ts",
        "description": "File: src/runs/pr-annotate.ts, lines ~200-228.\n\n`buildHeaders`, `readJson`, `readText`, and `truncate` are defined privately in pr-annotate.ts with identical bodies to the exported versions in src/runs/pr-checks.ts.\n\nFix: delete the four private definitions in pr-annotate.ts and import them from `./pr-checks.ts` instead. No behavior change — the implementations are byte-identical.\n\nAdditionally, src/ci-fixer/check-runs.ts has its own private `buildHeaders` (missing `content-type`) and `readText`. These are GET-only calls so the missing `content-type` is benign, but the `readText` implementation is identical to pr-checks.ts. Import `readText` from `../runs/pr-checks.ts` in check-runs.ts (adjust path as needed). Leave check-runs.ts's private `buildHeaders` in place since it deliberately omits `content-type` for GET-only endpoints (document this with a one-line comment if not already present).",
        "type": "task",
        "priority": 3,
        "blocks": [],
        "labels": ["nightwatch"]
      },
      {
        "title": "Consolidate env-parsing helpers shared between preview-eviction and gc",
        "description": "Files: src/preview/eviction/config.ts and src/runs/reap/gc.ts.\n\nBoth files define private `parseEnvDuration`, `parseEnvPositiveInt`, and `isTruthy` with identical implementations. `src/server/main/utils.ts` already exports `parseTrueEnv` which is identical to `isTruthy`.\n\nFix:\n1. Extract `parseEnvDuration` and `parseEnvPositiveInt` into a shared internal module. The best home is a new file `src/server/main/env-parse.ts` (or add to `src/server/main/utils.ts` if it doesn't blow the size budget).\n2. In `preview/eviction/config.ts`: delete the three local helpers, import `parseEnvDuration` and `parseEnvPositiveInt` from the shared location, replace `isTruthy` calls with `parseTrueEnv` from `src/server/main/utils.ts`.\n3. In `runs/reap/gc.ts`: same substitutions.\n4. Verify tests pass.",
        "type": "task",
        "priority": 3,
        "blocks": [],
        "labels": ["nightwatch"]
      },
      {
        "title": "Deduplicate supervisor git-runner utility",
        "description": "Files: src/supervisor/git-credentials.ts and src/supervisor/git-identity.ts.\n\n`defaultGitCredentialsRun` (lines 84-97) and `defaultGitIdentityRun` (lines 110-123) have byte-identical implementations — both wrap `execFileAsync` with the same error-shape extraction pattern.\n\nFix: extract a shared `makeDefaultGitRun` factory in a new file `src/supervisor/git-run.ts` (or add to a supervisor `utils.ts`). The factory takes no arguments and returns a function matching the shared `(cmd, args) => Promise<{exitCode, stdout, stderr}>` shape. Both `git-credentials.ts` and `git-identity.ts` call `makeDefaultGitRun()` to obtain their respective `default*Run` constant.",
        "type": "task",
        "priority": 3,
        "blocks": [],
        "labels": ["nightwatch"]
      },
      {
        "title": "Extract read-yaml-validate helper inside warren-config/load.ts",
        "description": "File: src/warren-config/load.ts, lines 227-256 and 330-359.\n\nThe read-file → trim → YAML-parse → validate-against-schema pattern is duplicated twice in the same file. The only difference between the two blocks is the validator function called at the end (`parseConfigFile` vs `parsePreviewFile`).\n\nFix: extract a private `loadYamlFile<T>(input: LoadInput, absPath: string, relPath: string, parse: (doc: unknown) => ParseResult<T>): Promise<T | null>` helper. Replace both duplicated blocks with calls to this helper. No behavior change.",
        "type": "task",
        "priority": 3,
        "blocks": [],
        "labels": ["nightwatch"]
      },
      {
        "title": "Extract resolveTargetDir CLI helper shared between config-migrate and init",
        "description": "Files: src/cli/commands/config-migrate.ts and src/cli/commands/init.ts.\n\n`resolveTargetDir` is defined with an identical body in both files (lines 169-192 in config-migrate.ts, lines 159-183 in init.ts). The only difference is the `deps` type (ConfigMigrateDeps vs InitDeps), but both satisfy the same structural requirement: `deps.projects.get(id): Promise<{localPath: string} | null>`.\n\nFix: extract `resolveTargetDir` into a shared `src/cli/commands/shared.ts` (or add to an existing shared helpers module if one exists). Use a structural interface for `deps` so both callers can pass their own `deps` type without a common base class.",
        "type": "task",
        "priority": 3,
        "blocks": [],
        "labels": ["nightwatch"]
      },
      {
        "title": "Consolidate server handler test harness boilerplate",
        "description": "Files: src/server/handlers/projects.test-helpers.ts and src/server/handlers/runs.test-helpers.ts.\n\nThese two files share 135 lines of identical test infrastructure (poolFor, silentLogger, stub, BurrowFixture interface, and makeBurrowClient). This means any change to the shared stubs must be applied twice.\n\nFix: create `src/server/handlers/test-helpers.ts` (or `src/server/test-helpers.ts`) that exports the shared helpers. Update `projects.test-helpers.ts` and `runs.test-helpers.ts` to import and re-export from the new shared module rather than defining duplicates. Tests that already import from these files will continue to work without changes.\n\nAlso consolidate the overlapping blocks between plots.test-support.ts and plots.workbench.harness.ts (3 separate clone blocks: lines 56-70, 82-102, 177-213 in test-support.ts matching blocks in workbench.harness.ts) using the same shared-and-re-export pattern.",
        "type": "task",
        "priority": 4,
        "blocks": [],
        "labels": ["nightwatch"]
      }
    ],
    "acceptance": [
      "bun test passes with no failures after each step",
      "bun run lint passes (biome --error-on-warnings)",
      "bun run typecheck passes (tsc --noEmit)",
      "For step 1: new unit test covering baseline=[pl-old] workspace=[pl-new] scenario passes and returns autoPlanRunCreated=true",
      "For steps 2-8: no import from a local private formatError/buildHeaders/etc. definition where a shared export is now available"
    ]
  }
}
